generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
}

// primeiro prototipo de model
model User {
    id          Int         @id @default(autoincrement())
    name        String
    email       String      @unique
    createdAt   DateTime    @default(now())
}

